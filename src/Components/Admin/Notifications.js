import React from 'react';
import { View, Text } from 'react-native';

const Notifications = () => (
  <View>
    <Text>Notifications & Announcements Section</Text>
    {/* Send announcements, view history UI here */}
  </View>
);

export default Notifications;
